describe('favoritesX', function() {

	beforeEach(module('jne'));

	it('should ...', inject(function($filter) {

        var filter = $filter('favoritesX');

		expect(filter('input')).toEqual('output');

	}));

});